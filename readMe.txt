Design
- Create basic html structure 
- Style the page using CSS
Functionality
- Create basic logic 
    -Get user input
        -get user input from bill-amount
        -get user input from service-rating
        -get user input from split-amount
    - Calculate tip
        - Math.round (((bill-amount * service-rating) + bill-amount) / split-amount)
    - Provide the calculated tip to the user
